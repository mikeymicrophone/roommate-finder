<table>
	<% @payees.each do |p| %>
		<tr>
			<td><%= link_to p.name, p %></td>
			<td><%= link_to pluralize(p.bills.count, 'bill'), payee_bills_path(p) %></td>
			<td><%= p.service %></td>
		</tr>
	<% end %>
</table>
<%= link_to 'new payee', new_payee_path %>